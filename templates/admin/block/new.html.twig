{% extends '/admin/base.html.twig' %}

{% block title %}{{ 'global.new_post'|trans() }}{% endblock %}

{% block content %}
    {{ parent() }}
    <a class="hms-color1" href="{{ path('block_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

    {{ include('admin/block/_form.html.twig') }}

    <a class="hms-color1" href="{{ path('block_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>
{% endblock %}
