{% extends '/admin/base.html.twig' %}

{% block title %}{{ 'global.new_post'|trans() }}{% endblock %}

{% block content %}
    {{ parent() }}
    <a href="{{ path('block_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

    {{ include('admin/block/_form.html.twig') }}

    <a href="{{ path('block_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>
{% endblock %}
