{% extends '/admin/base.html.twig' %}

{% block title %}{{ 'block.edit'|trans() }}{% endblock %}

{% block content %}
    {{ parent() }}
    <a href="{{ path('block_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>
    {{ include('admin/block/_form.html.twig', {'button_label': 'global.update'|trans()}) }}
    {{ include('admin/block/_delete_form.html.twig') }}

    <a href="{{ path('block_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

{% endblock %}