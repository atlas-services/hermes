{% if section is not null %}
    {% if section.id is not null %}
        {{ include('admin/_delete_form.html.twig', {'objet': 'global.delete_section'|trans() ~ ' "'~ section.name ~ '" ?', 'action': path('section_delete', {'id': section.id}), 'csrf_token_delete': csrf_token('delete' ~ section.id) }) }}
    {% endif %}
{% endif %}
