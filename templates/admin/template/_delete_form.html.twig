{{ include('admin/_delete_form.html.twig', {'objet': 'global.delete_content'|trans() ~ ' "'~ hms_template.name ~ '" ?', 'action': path('template_delete', {'id': hms_template.id}), 'csrf_token_delete': csrf_token('delete' ~ hms_template.id) }) }}

