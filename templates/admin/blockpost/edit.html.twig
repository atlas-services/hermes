{% extends '/admin/base.html.twig' %}

{% block title %}Edit blockpost{% endblock %}

{% block content %}
    {{ parent() }}
    <a href="{{ path('blockpost_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

    {{ include('admin/blockpost/_form.html.twig', {'button_label': 'global.update'|trans()}) }}

    {{ include('admin/blockpost/_delete_form.html.twig') }}

    <a href="{{ path('blockpost_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

{% endblock %}
