{% extends '/admin/base.html.twig' %}

{% block title %} {{ 'global.edit'|trans()|raw }}{% endblock %}

{% block nav %}
    <section id="section_top" class="container"></section>
    {% include '/admin/customer/nav.html.twig' %}
{% endblock nav %}

{% block content %}
    <a href="{{ path('order_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>
    <h1 class="text-success">{{ 'order.label'|trans() }}</h1>

    {{ include('admin/order/_form.html.twig', {'button_label': 'global.update'|trans()}) }}
    {% if is_granted('ROLE_ADMIN') %}
        {{ include('admin/order/_delete_form.html.twig') }}
    {% endif %}

    <a href="{{ path('order_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

{% endblock %}