{{ include('admin/_delete_form.html.twig', {'objet': 'global.delete_menu'|trans() ~ ' "'~ menu.name ~ '" ?', 'action': path('menu_delete', {'id': menu.id}), 'csrf_token_delete': csrf_token('delete' ~ menu.id) }) }}

