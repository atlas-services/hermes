{% extends '/admin/base.html.twig' %}

{% block title %} {{ 'menu.edit'|trans() }}{% endblock %}

{% block content %}
    {{ parent() }}
    <a class="hms-color1" href="{{ path('menu_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

    {{ include('admin/menu/_form.html.twig', {'button_label': 'global.update'|trans()}) }}
    {{ include('admin/menu/_delete_form.html.twig') }}

    <a class="hms-color1" href="{{ path('menu_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

{% endblock %}