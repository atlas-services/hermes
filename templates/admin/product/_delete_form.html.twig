{{ include('admin/_delete_form.html.twig', {'objet': 'global.delete_content'|trans() ~ ' "'~ product.name ~ '" ?', 'action': path('product_delete', {'id': product.id}), 'csrf_token_delete': csrf_token('delete' ~ product.id) }) }}

