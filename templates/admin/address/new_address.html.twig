{% block content %}

  {% if (form.countryCode.vars['choices']['0'].value) == 'FR' %}
    {{ include('admin/address/address_form_new_fr.html.twig') }}
  {% else %}
    {{ include('admin/address/address_form_new.html.twig') }}
  {% endif %}

{% endblock %}
