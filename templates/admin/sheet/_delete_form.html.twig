{{ include('admin/_delete_form.html.twig', {'objet': 'global.delete_sheet'|trans() ~ ' "'~ sheet.name ~ '" ?', 'action': path('sheet_delete', {'id': sheet.id}), 'csrf_token_delete': csrf_token('delete' ~ sheet.id) }) }}
    