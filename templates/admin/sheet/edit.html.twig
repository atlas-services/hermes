{% extends '/admin/base.html.twig' %}

{% block title %} {{ 'sheet.edit'|trans() }}{% endblock %}

{% block content %}
    <a class="btn h-btn-outline-success mb-sm-3"  href="{{ path('sheet_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>
    <h1 class="text-secondary">{{ 'sheet.edit'|trans() }}</h1>

    {{ include('admin/sheet/_form.html.twig', {'button_label': 'global.update'|trans()|raw}) }}
    {{ include('admin/sheet/_delete_form.html.twig') }}

    <a class="btn h-btn-outline-success mb-sm-3"  href="{{ path('sheet_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

{% endblock %}