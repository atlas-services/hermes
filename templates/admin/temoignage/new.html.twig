{% extends '/admin/base.html.twig' %}

{% block title %}{{ 'global.new_page'|trans() }}{% endblock %}

{% block content %}
    {{ parent() }}
    <a class="btn h-btn-outline-success"  href="{{ path('temoignage_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>

    {{ include('admin/temoignage/_form.html.twig') }}

    <a class="btn h-btn-outline-success"  href="{{ path('temoignage_index') }}">{{ 'global.back_to_list'|trans()|raw }}</a>
    </div>
{% endblock %}