{{ include('admin/_delete_form.html.twig', {'objet': 'global.delete_content'|trans() ~ ' "'~ post.name ~ '" ?', 'action': path('post_delete', {'id': post.id}), 'csrf_token_delete': csrf_token('delete' ~ post.id) }) }}
